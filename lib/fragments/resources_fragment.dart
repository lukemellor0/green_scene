import 'package:flutter/material.dart';

class ResourcesFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Hello Resources"),
    );
  }

}